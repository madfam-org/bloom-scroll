# Stability Session Wrap-Up - 2026-05-28

This file records the end state of the 2026-05-28 stabilization session. Use
`docs/CURRENT_STATE.md` for the living current-state reference; this file is the
session-specific evidence trail.

## Scope

- Audited repository docs, backend, frontend, CI/CD, Kubernetes manifests, and
  production behavior.
- Verified production through Enclii-first operations and public HTTP smoke
  checks against `almanac.solar` and `api.almanac.solar`.
- Updated tests and docs before committing and pushing.
- Avoided raw production infrastructure access; no break-glass command was
  needed.

## Final Deployed State

- Final deployed commit: `a84a3de0b614f1fc4fb2cc0a15fd846d490c02eb`.
- Final functional remediation commit: `5eb52596d4d6dd079c1324084b8147cb6fa92f17`.
- Digest-pin commit generated by CI: `a84a3de0b614f1fc4fb2cc0a15fd846d490c02eb`.
- Production API image: `ghcr.io/madfam-org/bloom-scroll/api@sha256:70368afa2bd624b4ed61b12b23a7c0004f1f6befca74eb2ee6278598c9fcf84e`.
- Production web image: `ghcr.io/madfam-org/bloom-scroll/web@sha256:05c726939e02a7a9a35ffb1d398f104a5848abd353a7e968ba4c23dc2fd4ca96`.
- This wrap-up may be followed by a docs-only commit. Production remains on
  `a84a3de` until a later non-documentation deploy changes it.

## Work Completed

### Production and Control Plane

- Confirmed `almanac.solar` serves the Flutter web shell.
- Confirmed `api.almanac.solar/health` returns healthy.
- Confirmed production `/docs`, `/redoc`, and `/openapi.json` are hidden.
- Confirmed finite-feed completion still returns `"The Garden is Watered."`.
- Confirmed the production Flutter bundle is baked to
  `https://api.almanac.solar/api/v1`.
- Verified released Enclii CLI `v1.0.0-alpha.1` reports service runtime health
  correctly from the distributed artifact.
- Verified ArgoCD application `bloom-scroll-services` is `Healthy`, `Synced`,
  and reconciled at `a84a3de0b614f1fc4fb2cc0a15fd846d490c02eb`.

### Backend Dependency Determinism

- Committed `backend/poetry.lock` for deterministic standard backend installs.
- Kept heavy ML wheels out of Poetry resolution to avoid cross-platform
  `torch` and CUDA drift.
- Added `backend/requirements-ml-linux-cpu.txt` for production Linux images:
  `torch==2.2.2+cpu`, `sentence-transformers==5.5.1`, and
  `transformers==4.57.6`.
- Added `backend/requirements-ml-local.txt` for local embedding work.
- Updated `backend/Dockerfile` so CPU-only ML wheels install before Poetry and
  before application code is copied.
- Added `backend/tests/test_dependency_lock.py` to prevent Poetry from
  reintroducing `torch`, `triton`, `transformers`, `sentence-transformers`, or
  `nvidia-*` packages into the standard dependency graph.

### API and Data Improvements

- Added Janua RS256/JWKS verification coverage, including valid-key,
  unknown-`kid`, and explicit legacy HS256 fallback tests.
- Added a repo-owned OpenAlex ingestion connector and API coverage for work
  normalization, abstract reconstruction, authors, concepts, citation counts,
  source IDs, and malformed-work rejection.
- Repaired backend tests to current module names and API behavior.

### CI/CD and Docs

- CI now validates root Compose config, backend lint/type/tests, Flutter
  analyze/tests/build, NetworkPolicy ports, and Docker image builds.
- Build & Deploy runs the shared Enclii build/publish workflow and follows with
  `scripts/prod-smoke.sh`.
- Updated docs to reflect production docs hiding, Enclii release verification,
  deterministic backend installs, CPU-only ML wheel handling, and remaining
  stability priorities.

## Verification Evidence

### Local

- `poetry check` passed.
- `poetry run ruff check .` passed.
- `poetry run mypy . --ignore-missing-imports` passed across 37 files.
- `poetry run pytest -q` passed 28 tests.
- `poetry run pytest -q tests/test_dependency_lock.py` passed.
- `git diff --check` passed.
- `docker build --target builder -f backend/Dockerfile backend` succeeded and
  installed CPU-only ML wheels from `requirements-ml-linux-cpu.txt`.

### GitHub Actions

- CI run `26608492278` completed successfully for `5eb5259`.
- Build & Deploy run `26608492358` completed successfully for `5eb5259`.
- The deploy workflow produced digest-pin commit `a84a3de` and production smoke
  passed in the workflow.

### Production

- `scripts/prod-smoke.sh` passed locally after the `a84a3de` rollout.
- Released Enclii CLI status reported:
  - `bloom-scroll-web` running, healthy, `2/2`, version `argocd-a84a3de`.
  - `bloom-scroll-api` running, healthy, `2/2`, version `argocd-a84a3de`.
- Released Enclii CLI app status reported:
  - app: `bloom-scroll-services`
  - health: `Healthy`
  - sync: `Synced`
  - operation phase: `Succeeded`
  - revision: `a84a3de0b614f1fc4fb2cc0a15fd846d490c02eb`
  - finished at: `2026-05-28T23:44:31Z`

## Remaining Stability Priorities

1. Frontend E2E and stress coverage:
   cover finite-feed completion, pagination, production API-base behavior,
   missing metadata, image/aspect-ratio failures, and error-boundary paths.
2. Production observability:
   add error telemetry, uptime checks, latency/error dashboards, feed failure
   alerts, ingestion alerts, and browser error reporting.
3. Load and soak testing:
   benchmark feed, health, and ingestion paths under realistic concurrency and
   with larger vector/card counts.
4. Runtime resilience:
   add API/client retry behavior, app-level rate limiting, hot-feed caching, and
   graceful degradation tests for database or ingestion outages.
5. Data and product hardening:
   finish real bias/constructiveness scoring, formalize user data export/delete
   behavior, and replace Celery scaffold paths that still return
   `not_implemented`.

## Session Closure

The production alpha is stable enough to move from dependency/deploy
remediation into user-flow regression coverage and observability. No known
urgent production defect remained at session close.
