# Optional local ML runtime for embedding development outside Linux images.
# The Docker image uses requirements-ml-linux-cpu.txt instead.
torch==2.12.0+cpu
sentence-transformers==5.5.1
transformers==5.10.2
